export default function CheckoutPage() {
    return (
        <main className="p-8 max-w-2xl mx-auto">
            <h1 className="text-3xl font-black italic">Checkout</h1>
            <p className="mt-4 text-zinc-400">Secure payment via SpaceRemit.</p>
            {/* Checkout form logic moves here during migration */}
        </main>
    );
}
