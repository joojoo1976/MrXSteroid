services:
  008mrxsteroidlandingpage:
    image: 008mrxsteroidlandingpage
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
